version: 2
jobs:
  one:
    docker:
      - image: circleci/ruby:2.4.1
    steps:
      - checkout
      - run:
          name: storing val
          command: CT=${CIRCLE_TAG}
      - run:
          name: echo val
          command: echo $CT
workflows:
  version: 2
  one:
    jobs:
      - one
